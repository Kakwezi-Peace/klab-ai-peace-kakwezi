# Day 1 Reflection

**1. Which concept clicked immediately today?**

Some Concepts clicked fast because I somehow had some basics to relate to. The four collections (list, tuple, dict, set) made sense right away since most of the languages have Data structures though names may varry e.g Java has Arrays, ArrayLists, LinkedLists, Sets, and Maps yes different names, same idea of storing and organizing data.    Writing the three functions (normalise, summarise_scores, safe_divide) was a bit different like syntax-wise since I am used to Java's data types and access modifiers (public/private....), but the underlying logic was familiar. NumPy arrays clicked too, since they're basically like arrays in Java an organized structure for storing numbers. The chart with Matplotlib also came easily, since I took Big Data course at AUCA where I used NumPy, Pandas, Matplotlib, scikit-learn, and TensorFlow/PyTorch before, including plotting graphs with X/Y axes. Same with f-strings printing formatted output with variables inserted directly into a string is a pattern I had seen before, just with different syntaxis what was somehow new.


**2. Which concept is still fuzzy, and what specifically confuses you about it?**

Broadcasting in NumPy still confuses me matrix - column_means works even though the shapes look different, and I don't fully get why. sys.path.append("..") is another one; I know it lets the notebook find the src folder, but not exactly how Python decides where to look. 
Comprehensions like [s for s in scores if s > 0.85] still read backwards to me, since I keep expecting the if to come first. The (hi - lo) or 1 trick in normalise() avoids dividing by zero, but I don't fully understand why or 1 works that way. And with Pandas, .info(), .describe(), and filtering all ran fine, but I couldn't confidently explain what's happening internally.

**3. One question you want answered at the start of tomorrow's session.**
Okay i would like to understand three things better that were somehow confusing to me : like how to decide what goes on the X-axis versus the Y-axis when plotting a chart? and also is Jupyter Notebook and notebooks/ opened inside VS Code truly the same thing or do they perform the same purpose? or if there's a real difference. the last one, since Python has no public/private.... like Java, how do functions actually get called across different files and folders, and whether there's any real convention professional way to use to protect internal code when it's imported elsewhere?


**4. Roughly how many hours did this assignment take you?**

This assignment took me around 5.5hrs because i had to first read and make research on hard concepts that were challenging to me then i had to later do the assignment which roughly took those hrs.